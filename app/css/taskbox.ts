import { StyleSheet } from "react-native";

export const taskboxStyles = StyleSheet.create({
    box : {
        backgroundColor : '#1abc9c',
        padding : 20,
        borderRadius : 5,
        justifyContent: 'flex-start',
        alignItems: 'flex-start',
    },
});
