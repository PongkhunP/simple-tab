import { StyleSheet } from "react-native";

export const noteStyles = StyleSheet.create({
    notebox : {
        flex: 1,
        padding: 20,
        backgroundColor: "red",
        gap: 10
    }
});
